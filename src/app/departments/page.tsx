import { BackOfficePlaceholderPage } from "@/src/components/layout/BackOfficePlaceholderPage";

export default function DepartmentsPage() {
  return (
    <BackOfficePlaceholderPage
      activeItem="departments"
      title="Departments"
      description="Organize products into store departments for catalog and register workflows."
    />
  );
}
