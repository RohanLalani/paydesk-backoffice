{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main window to use only Tauri core defaults.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
